/**
 * The type of (redux) action which sets the last-n for the conference.
 *
 * {
 *     type: SET_LAST_N,
 *     lastN: number
 * }
 */
export const SET_LAST_N = "SET_LAST_N";

/**
 * The type of (redux) action which sets the speaker tag for the conference.
 *
 * {
 *     type: SET_SPEAKER_TAG,
 *     speakerId: string
 * }
 */
export const SET_SPEAKER_TAG = "SET_SPEAKER_TAG";
